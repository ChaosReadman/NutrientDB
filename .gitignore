# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below if it's a vendor-managed project)
# vendor/

# Air temporary directory
tmp/

# SQLite database (generated from XML)
data/*.db

# OS specific files
.DS_Store
